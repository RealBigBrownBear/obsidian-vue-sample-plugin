# Exclude macOS Finder (System Explorer) view states.
# 排除 macOS Finder (系统资源管理器) 的视图状态文件.
.DS_Store

# Exclude the IDE configuration files.
# 排除 IDE 配置文件.
.vscode
.idea 
*.iml

# Exclude the dependencies of the package.
# 排除项目依赖文件.
node_modules

# Exclude sourcemaps.
# 排除 sourcemaps.
*.map

# Exclude data.json file automatically generated by Obsidian.
# 排除由 Obsidian 自动生成的 data.json 文件.
data.json

# Don't include the compiled main.js file in the repo.
# It should be uploaded to GitHub releases instead.
# 不要将编译好的 main.js 文件混杂在仓库的代码文件里.
# 它应该出现在 GitHub 的 releases 当中.
main.js
